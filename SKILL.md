---
name: paper-plagiarism-check
description: 论文/文档查重的本地检测工具。将一篇论文(docx/pdf/txt/md)与用户提供的参考文献、往稿或语料库比对,输出总重复率、分来源重合度和每一处重复片段(含段落定位与双方原文对照)。Use whenever the user mentions 论文查重, 查重, 重复率, plagiarism check, duplicate check, similarity between documents, self-plagiarism, 文本重合, or asks to check whether a paper copies from given references — even if they just hand over a paper and a reference folder without saying 查重.
---

# Paper Plagiarism Check(论文查重)

本地词元比对:把论文与参考文献都归一化成词元序列(中文逐字、英文按词,忽略大小写/全半角/空白/标点),凡与某篇参考文献存在 **k 个连续相同词元**(默认中文 13、英文 6,即常见的"连续 13 字判重"思路)即计为重复,合并为最大片段后生成报告。全程本地运行,文本不出本机。

## 先说清楚能力边界(必须告知用户)

- 这是**本地预检**,只比对用户提供的参考文献,看不到知网/Turnitin/维普的数据库,也不会联网检索。结果应表述为"与所给参考文献的重复率",**绝不能**说成"知网重复率"。
- 同义改写、语序调整、中英互译后的雷同**检不出**;检测的是逐字重合。
- 重复片段需人工确认:文献综述里的专有名词串、各论文通用的固定表述可能误报。

## 工作流程

1. **收集输入。** 找到论文文件与参考文献。支持 `.docx` `.txt` `.md`,PDF 需要 `pip install pypdf`。参考文献可以是任意文件/目录的组合(引文原文、用户自己的往稿、课程提供的语料库等)。**若用户没给参考文献,先问清楚要比对什么**——没有参照物,查重无从谈起。
2. **运行脚本**(脚本在本技能目录 `scripts/check_plagiarism.py`,可在任意工作目录运行):
   ```
   python "<本技能目录>/scripts/check_plagiarism.py" --paper <论文文件> --refs <文献1> <文献2或目录...> --out 查重报告.md --html 查重报告.html --json 查重结果.json
   ```
   常用参数:`--k 13` 指定判定窗口(默认自动:中文 13 / 英文 6);`--min-run 20` 过滤过短片段;纯英文文献建议显式 `--k 6`。
3. **读取报告并在对话里总结**:总重复率与档位(低 <10% / 中 10–30% / 高 >30%,注明这只是本地工具的经验档位)、各来源重合表、以及**最长的 3–5 处重复**(给出段落号,方便用户定位修改)。
4. **交付文件**:把 `查重报告.html`(论文全文高亮)和 `查重报告.md` 的路径给用户。

## 结果解读要点

- 总重复率 = 论文词元中出现在任一参考文献里的比例(各来源取并集,不重复计数)。
- 明细里每处片段给出论文原文(加粗)与参考文献原文对照;若两边只差标点/空格,属于归一化设计而非误报,可如实说明。
- 发现大量通用套话的短片段时(如"随着…的快速发展"),建议加 `--min-run` 提高片段最小长度后重跑。
- 若参考文献里混入论文自身或旧版全文,脚本会检测到内容完全相同并自动跳过、给出警告。

## 网页版(两种模式)

用户想要图形界面时,启动本地网页服务:

```
python "<本技能目录>/scripts/webapp.py" --port 8765        # 自动打开浏览器;--no-open 禁止
```

纯标准库实现,零依赖;默认只监听 127.0.0.1。页面有三种模式:

- **在线检索查重(默认)**:只需拖入论文,**无需准备参考文献**。自动切句后逐句到
  OpenAlex(约 2.5 亿篇题录摘要)、Europe PMC(开放获取全文)与搜索引擎
  (360/搜狗,英文用 Bing)检索原句出处,命中后抓取原文**逐字核验**,只把核验
  通过的计入重复率。任务制执行,页面有进度条;结果含来源链接、原文片段、全文
  高亮与可下载的 HTML 报告。检测逻辑在 `scripts/online_check.py`。
- **与本地文献比对**:原有的拖拽文献/文件夹比对模式(判定窗口 k 等高级选项)。
- **AIGC 疑似分析**:离线启发式统计(秒级),估计"AI 生成风格疑似度"0-100。
  信号包括:句长均匀度(burstiness)、模板短语密度("随着…的快速发展""综上所述"
  "值得注意的是" / "with the rapid development of" 等)、连接词开头句占比、
  模糊限定词密度、顿号列举密度;逐句橙色/黄色标注 + 可下载 HTML 报告。
  逻辑在 `scripts/aigc_check.py`。**必须强调:这是启发式统计,不构成 AI 写作的
  证明——人写的模板化文章也会得分,AI 写的朴素句子也可能低分,商用检测器同样
  有误判;仅作写作风格自查参考。**

## 在线检索的如实边界(必须告知用户)

- 只能检索**开放数据源**:商业库(知网/万方/维普)的授权全文无法检索;网页渠道受
  搜索引擎索引与排序限制,是**尽力检索**,不保证发现所有出处。
- 判定是整句(或较长小句)逐字重复;同义改写、语序调整不会命中。
- 检索频次受各数据源限流约束(自动退避重试、超限自动停用网页来源),百句级论文
  约需几分钟。
- 结果是参考指标,**不能表述为"知网重复率"**。

## 故障排查

- 提示需要 pypdf → `pip install pypdf`,或让用户把 PDF 另存为 .docx/.txt(扫描版 PDF 无文字层,提取为空,脚本会警告)。
- 某参考文献词元数为 0 或极少 → 可能是扫描件/图片型文档。
- 中文 txt 乱码 → 脚本自动尝试 UTF-8/GBK/Big5;仍乱码时请用户另存为 UTF-8。
- `.docx` 报"不是有效的 .docx" → 多半是 `.doc` 改了扩展名,请用户在 Word 里另存为真正的 .docx。
